<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coderbd.noticeboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NoticeActivity"></activity>
        <activity android:name=".UserListActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".LoginAll">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TeacherDashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StudentDashboard" /> <!-- <activity android:name=".LoginSignup"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="@string/title_activity_login_signup" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".NoticeListActivity" />
        <activity android:name=".StudentProfileActivity" />
        <activity android:name=".StudentNoticeListActivity" />
        <activity android:name=".TeacherNoticeListActivity" />
        <activity android:name=".TeacherProfileActivity" />
        <activity android:name=".TeacherNoticeActivity" />
    </application>

</manifest>